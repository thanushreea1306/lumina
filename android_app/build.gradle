// Root build file for the LUMINA Android project.
// Plugin versions are declared here (with `apply false`) so the app module can
// use them without each module re-resolving a version.
plugins {
    id 'com.android.application' version '8.4.2' apply false
    id 'org.jetbrains.kotlin.android' version '1.9.24' apply false
}
